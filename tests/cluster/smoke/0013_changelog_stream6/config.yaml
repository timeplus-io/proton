name: changelog_stream6
description: Tests covering the single shard changelog stream query smoke cases.
tags:
  - smoke
setup:
  scope: case
  steps:
  - type: query
    sql: DROP VIEW IF EXISTS test14_view_6;
  - type: wait
    time: 1

  - type: query
    sql: DROP STREAM IF EXISTS test14_substream_6;
  - type: wait
    time: 1
