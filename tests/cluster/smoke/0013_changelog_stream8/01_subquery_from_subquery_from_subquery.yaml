tags:
  - versioned_kv_subquery/view/materialized_view
  - global_aggregation
  - group_by_primary_key
  - skip
description: subquery from subquery from subquery
cluster:
  - p1k1
  - p3k1
steps:
  - type: query
    sql:
      create stream test14_subquery_8(i float, k1 int, k2 string) primary
      key k2 settings mode='versioned_kv';
  - type: wait
    time: 2

  - name: "1449"
    type: stream
    query: |
      with cte1 as (with cte2 as (select i, k1, k2, _tp_delta from test14_subquery_8)
      select * from cte2) select max(i), min(i), avg(i), k2 from cte1 group by k2;
    schema:
      - name: max(i)
        type: float32
      - name: min(i)
        type: float32
      - name: avg(i)
        type: float64
      - name: k2
        type: string
  - type: wait
    time: 3

  - type: query
    sql: insert into test14_subquery_8(i, k1, k2) values (1, 2, 'a');

  - type: wait
    time: 3

  - type: query
    sql: insert into test14_subquery_8(i, k1, k2) values (3, 3, 'b');

  - type: wait
    time: 3

  - type: query
    sql: insert into test14_subquery_8(i, k1, k2) values (5, 1, 'c');

  - type: wait
    time: 3

  - type: query
    sql: insert into test14_subquery_8(i, k1, k2) values (8, 2, 'a');

  - type: wait
    time: 5

  - type: query
    sql: insert into test14_subquery_8(i, k1, k2) values (9, 3, 'b');

  - type: wait
    time: 5

  - type: query
    sql: insert into test14_subquery_8(i, k1, k2) values (11, 2, 'b');

  - type: wait
    time: 5

  - type: query
    sql: insert into test14_subquery_8(i, k1, k2) values (10, 1, 'c');

  - type: wait
    time: 5

  - type: query
    sql: insert into test14_subquery_8(i, k1, k2) values (14, 2, 'b');

  - type: check
    target_name: "1449"
    mode: random
    expected_result:
      - [1, 1, 1, a]
      - [3, 3, 3, b]
      - [1, 1, 1, a]
      - [3, 3, 3, b]
      - [5, 5, 5, c]
      - [1, 1, 1, a]
      - [3, 3, 3, b]
      - [5, 5, 5, c]
      - [8, 8, 8, a]
      - [9, 9, 9, b]
      - [5, 5, 5, c]
      - [8, 8, 8, a]
      - [11, 11, 11, b]
      - [5, 5, 5, c]
      - [8, 8, 8, a]
      - [11, 11, 11, b]
      - [10, 10, 10, c]
      - [8, 8, 8, a]
      - [14, 14, 14, b]
      - [10, 10, 10, c]
      - [8, 8, 8, a]
