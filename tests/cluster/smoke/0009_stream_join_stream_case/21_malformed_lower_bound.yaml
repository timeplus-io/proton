tags:
  - stream_join_stream
description: lower bound shall be less than or equal to 0
cluster:
  - p1k1
  - p3k1
steps:
  - name: '1022'
    type: stream
    query: select s, ss from test10_left_stream join test10_right_stream on i=ii and
      date_diff('second', ts, tts) > 3 and date_diff('second', ts, tts) < 10
    schema:
      - name: s
        type: string
      - name: ss
        type: string

  - type: check
    target_name: '1022'
    expected_result:
      - [ error_code:62 ]
