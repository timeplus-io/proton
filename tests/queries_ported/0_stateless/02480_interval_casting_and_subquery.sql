SELECT to_interval_second(5) AS interval, to_datetime('2017-01-01 00:00:00') + interval AS res;
SELECT to_interval_minute(5) AS interval, to_datetime('2017-01-01 00:00:00') + interval AS res;
SELECT to_interval_hour(5) AS interval, to_datetime('2017-01-01 00:00:00') + interval AS res;
SELECT to_interval_day(5) AS interval, to_datetime('2017-01-01 00:00:00') + interval AS res;
SELECT to_interval_month(5) AS interval, to_datetime('2017-01-01 00:00:00') + interval AS res;
SELECT to_interval_quarter(5) AS interval, to_datetime('2017-01-01 00:00:00') + interval AS res;
SELECT to_interval_year(5) AS interval, to_datetime('2017-01-01 00:00:00') + interval AS res;
SELECT CAST(5 AS interval_nanosecond);
SELECT CAST(5 AS interval_microsecond);
SELECT CAST(5 AS interval_millisecond);
SELECT CAST(5 AS interval_second);
SELECT CAST(5 AS interval_minute);
SELECT CAST(5 AS interval_hour);
SELECT CAST(5 AS interval_day);
SELECT CAST(5 AS interval_week);
SELECT CAST(5 AS interval_month);
SELECT CAST(5 AS interval_quarter);
SELECT CAST(5 AS interval_year);
SELECT (SELECT to_interval_second(5)) AS interval, to_datetime('2017-01-01 00:00:00') + interval AS res;
SELECT (SELECT to_interval_minute(5)) AS interval, to_datetime('2017-01-01 00:00:00') + interval AS res;
SELECT (SELECT to_interval_hour(5)) AS interval, to_datetime('2017-01-01 00:00:00') + interval AS res;
SELECT (SELECT to_interval_day(5)) AS interval, to_datetime('2017-01-01 00:00:00') + interval AS res;
SELECT (SELECT to_interval_month(5)) AS interval, to_datetime('2017-01-01 00:00:00') + interval AS res;
SELECT (SELECT to_interval_quarter(5)) AS interval, to_datetime('2017-01-01 00:00:00') + interval AS res;
SELECT (SELECT to_interval_year(5)) AS interval, to_datetime('2017-01-01 00:00:00') + interval AS res;
