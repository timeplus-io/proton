SELECT to_low_cardinality(to_interval_second(1));
SELECT to_low_cardinality(to_interval_minute(1));
SELECT to_low_cardinality(to_interval_hour(1));
SELECT to_low_cardinality(to_interval_day(1));
SELECT to_low_cardinality(to_interval_week(1));
SELECT to_low_cardinality(to_interval_quarter(1));
SELECT to_low_cardinality(to_interval_year(1));
